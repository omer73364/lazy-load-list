# Lazy Load List (Svelte Example)

This is a basic example for Lazy Load List in Svelte js

## Installation

download the code then run `npm install` or `yarn`, 
to start the dev server: `npm run dev` or `yarn dev`
